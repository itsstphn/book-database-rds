{
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd backend && npm start\" \"cd client && npm start\""
  }
}
